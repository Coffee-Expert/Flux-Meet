{
  "version": 2,
  "builds": [
    {
      "src": "server/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/auth/(.*)",
      "dest": "routes/auth.js"
    },

    { "src": "/(.*)", "dest": "/" }
  ]
}
